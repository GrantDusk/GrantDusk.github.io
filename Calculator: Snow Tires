Input a number of snow tires and output how many people will have better winter driving

x = input('Snow Tires:')
x = (int(x))
people = (x/4)

def check():
	if x>3:
		print("drivers:", people)
	else:
		print("not enough tires.")

check()
